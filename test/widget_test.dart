import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'should return a valid test for the CI',
    () async {
      //arrange(setup).

      //act(run).

      //assert(verify).
      expect(2, (1 + 1));
    },
  );
}
